import React from "react";
import { Grid, Typography } from "@mui/material";

function MyData() {
  return (
    <div>
      <Typography variant="h1">My Data</Typography>
      Note: the data listed here is specific to the current project.
    </div>
  );
}

export default MyData;
